<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Basic permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Required permissions for Android 13+ notifications and exact alarms -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <application
        android:name=".activities.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MapMyTasks"
        >

        <activity android:name=".activities.ChatActivity" />

        <!-- Login screen – launcher activity -->
        <activity
            android:name=".activities.LogIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Application screens -->
        <activity android:name=".activities.Productivity" android:exported="false" />
        <activity android:name=".activities.WeeklySummary" android:exported="false" />
        <activity android:name=".activities.TasksScreen" android:exported="false" />
        <activity android:name=".activities.EditTask" android:exported="false" />
        <activity android:name=".activities.CreateTask" android:exported="false" />
        <activity android:name=".activities.PartnerManagement" android:exported="false" />
        <activity android:name=".activities.SentTasksActivity" android:exported="false" />
        <activity android:name=".activities.BalloonGameActivity" android:exported="false" />

        <!-- MainActivity must be exported=true to allow Intents -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true" />

        <!-- Foreground location service -->
        <service
            android:name=".data.LocationTaskService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- Receivers for notifications and geofencing -->
        <receiver android:name=".receivers.NotificationActionReceiver" />
        <!--  <receiver android:name=".receivers.TaskReminderReceiver" />-->

    </application>

</manifest>